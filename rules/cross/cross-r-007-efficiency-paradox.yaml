id: CROSS-R-107
title: Efficiency Paradox (Spend↑, HC↑, Orders↓)
severity: warn
bucket: productivity
version: "1.0.0"
evidence:
  checks:
    - type: cross_expr
      expr: "(pct_change(cmo.total_spend,1) > 0) & (pct_change(chro.headcount_total,1) > 0) & (pct_change(coo.orders_completed,1) < 0)"
      require_ratio_pass_pct: 0.5
fix_tip: "Recheck floor efficiency, role fit, and campaign→ops alignment."
