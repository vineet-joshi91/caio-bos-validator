id: CROSS-R-114
title: Attrition vs Recruitment Spend
severity: warn
bucket: cost_avoidance
version: "1.0.0"
evidence:
  checks:
    - type: cross_expr
      expr: "(pct_change(chro.attrition_rate,1) > 0) -> (pct_change(cpo.recruitment_spend,1) >= 0)"
      require_ratio_pass_pct: 0.6
fix_tip: "If attrition rises but recruiting doesn’t, expect delivery shortfalls."
