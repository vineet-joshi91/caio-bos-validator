id: CROSS-R-105
title: Hiring–Headcount Continuity
severity: warn
bucket: workforce
version: "1.0.0"
evidence:
  checks:
    - type: cross_expr
      expr: "(pct_change(cpo.offers_accepted,1) >= 0) -> (pct_change(chro.headcount_total,1) >= 0)"
      require_ratio_pass_pct: 0.7
fix_tip: "If headcount stalls despite offers, fix onboarding or plug early attrition."
